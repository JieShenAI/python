np.vstack((t1,t2)) ->竖直拼接
np.hstack((t1,t2)) ->水平拼接

竖直分割


行交换,不适用第3个变量交换2个变量的值
t[[1,2],:] = t[[2,1],:]
列交换
t[:,[0,2]] = t[:[2,0]]

获取最大值最小值的位置
	1.np.argmax(t,axis=0)
	
	2.np.argmin(t,axis=1)
	返回每行第一个最大值的下标
	(记法：axis所在轴 列的下标)
创建全为0的数组：
	np.zeros((3,4))
创建全为1的数组：np.ones((3,4))
创建一个对角线为1的正方形数组(方阵)：np.eye(3)